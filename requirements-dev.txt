# Dev / CI / deployment tooling (kept separate from the UTF-16 runtime
# requirements.txt to avoid encoding churn). Install with:
#   pip install -r requirements-dev.txt
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
gunicorn==23.0.0
ruff==0.8.4
