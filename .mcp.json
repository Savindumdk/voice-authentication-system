{
  "mcpServers": {
    "mongodb": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "mongodb-mcp-server", "--readOnly"],
      "env": {
        "MDB_MCP_CONNECTION_STRING": "${MONGODB_URI}"
      }
    }
  }
}
